name: quran
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_adaptive_scaffold: ^0.1.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/quran/page-images/

  fonts:
    - family: MeQuran
      fonts:
        - asset: assets/fonts/me_quran-Regular.ttf
        - asset: assets/fonts/me_quran-Regular.ttf
